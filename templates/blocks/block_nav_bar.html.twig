<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-lg">
		<a class="navbar-brand" href="{{ path('app_homepage') }}">{{ SITE_TITLE }}</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
		        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="#">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item  text-nowrap">
					<a class="nav-link" href="#">Features</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_about') }}">About</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_faq') }}">FAQ's</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
					   aria-haspopup="true" aria-expanded="false">More</a>
					<div class="dropdown-menu">
						{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
							<a class="dropdown-item text-nowrap" href="#">User Dashboard</a>
							<div class="dropdown-divider"></div>
						{% endif %}
						<a class="dropdown-item text-nowrap" href="{{ path('app_policy_acceptable_use') }}">Acceptable
							Use Policy</a>
						<a class="dropdown-item text-nowrap" href="{{ path('app_policy_privacy') }}">Privacy Policy</a>
						<a class="dropdown-item text-nowrap" href="{{ path('app_policy_tos') }}">Terms of Service</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item text-nowrap" href="#">Separated link</a>
					</div>
				</li>


				{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
					<li class="nav-item text-nowrap">
						<div class="nav-link">User: {{ app.user.first }}</div>
					</li>
					<li class="nav-item text-nowrap">
						<a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
					</li>

				{% else %}
					<li class="nav-item text-nowrap">
						<a href="{{ path('app_login') }}" class="nav-link">Log In</a>
					</li>
					<li class="nav-item text-nowrap">
						<a href="{{ path('app_signup') }}" class="nav-link">Sign up</a>
					</li>
				{% endif %}
			</ul>

		</div>
	</div>
</nav>