<fieldset>
    <legend class="legend">{{ nameLegend|default('Your NameSimple') }}</legend>
    {{ form_row(form.name.nameFirst) }}
    {{ form_row(form.name.nameMiddle) }}
    {{ form_row(form.name.nameLast) }}
    {{ form_row(form.name.nameSuffix) }}
</fieldset>
