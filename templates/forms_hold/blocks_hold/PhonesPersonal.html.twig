<fieldset>
    <legend class="legend">{{ Legend|default('Phone Number(s)') }}</legend>
    {{ form_row(form.phoneHome.phoneHome) }}
    {{ form_row(form.phoneMobile.phoneMobile) }}
    {{ form_row(form.phoneWork.phoneWork) }}
    {% if form.phoneFax.phoneFax is defined %}
        {{ form_row(form.phoneFax.phoneFax) }}
    {% endif %}

</fieldset>
