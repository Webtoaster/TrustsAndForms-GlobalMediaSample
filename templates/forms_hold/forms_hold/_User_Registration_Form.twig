<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            {{ form_start(form) }}

{#            {{ include('forms/blocks/NamePerson.html.twig') }}#}
            {{ include('forms/blocks/Email.html.twig') }}
            {{ include('forms/blocks/PasswordCreate.html.twig') }}
{#            {{ include('forms/blocks/ButtonSave.html.twig') }}#}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
