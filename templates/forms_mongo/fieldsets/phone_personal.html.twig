<fieldset>
	<legend class="legend">{{ Legend|default('') }}</legend>
	<div class="row">
		<div class="col">
			
			
			
			{{ form_row(form.phoneHome) }}
			{{ form_row(form.phoneMobile) }}
			{{ form_row(form.phoneWork) }}
		</div>
	</div>
</fieldset>